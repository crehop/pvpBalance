package PvpBalance;
import org.bukkit.Bukkit;
import org.bukkit.ChatColor;
import org.bukkit.entity.Player;
import org.bukkit.scoreboard.DisplaySlot;
import org.bukkit.scoreboard.Objective;
import org.bukkit.scoreboard.Score;
import org.bukkit.scoreboard.Scoreboard;
import org.bukkit.scoreboard.ScoreboardManager;

//ScoreboardManager manager = Bukkit.getScoreboardManager();
//Scoreboard board = manager.getMainScoreboard();
//board.registerNewObjective("Stamina", "SkillPoints");
//Objective objective = board.getObjective("Stamina");
//objective.setDisplayName(ChatColor.BLUE + "=== HEALTH ===");
//objective.setDisplaySlot(DisplaySlot.SIDEBAR);
//Score scoreName = objective.getScore(Bukkit.getOfflinePlayer( ChatColor.BLUE + "Power Level")); //Get a fake offline player
//scoreName.setScore(10000);
//Score score = objective.getScore(player);
//score.setScore(newScore); //Integer only!
//player.setScoreboard(board);
//if(Main.debug == true){
//	Bukkit.broadcastMessage("SCOREBOARD UPDATED");
public class DBZScoreBoard {
	public static void setScore(int newScore, Player player){
		
	}
}
